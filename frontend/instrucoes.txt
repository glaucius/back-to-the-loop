Vamos agora construir o frontend, que vai usar as mesmas tabelas, base de dados e bucket minio do serviço backoffice, logo, nosso produto final terá dois grandes modulos ou services, o frontend e o backoffice.

O frontend deve ter a seguinte especificação

- usar python + flask + jinja2 + SQLAlchemy do mesmo modo que o backoffice
- home: respeitar o menu como esta no site estatico, e veja que temos uma área logada e outra não logada
- no menu, o botão de logar e cadastrar deve permitir:
    - fazer o login usando as credenciais da base, tabela e datamodel atleta
    - permitir o auto cadastro de usuário, tabela e datamodel atleta
    - criar uma nova página para cadastro, uma nova view, pode usar como exemplo as paginas de backyard como exemplo
 - Usar como referencia de html, css e js o site estático de backyards na pasta "frontend-static-template" raiz do projeto

- adicionar novo service no docker-compose, e vamos fazer a mesma regra do backoffice, build, e restart para testar

acho que é isso por agora, depois vamos aprimorar